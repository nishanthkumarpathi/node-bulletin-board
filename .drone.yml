kind: pipeline
name: default


# Testing the Build and Push

steps:
  - name: build-push
    image: plugins/docker
    settings:
      username: 
        from_secret: DOCKER_USERNAME
      password:
        from_secret: DOCKER_PASSWORD
      repo: nishanthkp/node-app
      dockerfile: ./bulletin-board-app/Dockerfile
